# BharatZero local database target for future Prisma-backed reads.
# The MVP UI uses typed demo seed data and does not require a running database.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/bharatzero?schema=public"
